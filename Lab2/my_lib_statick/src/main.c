#include <stdio.h>
#include "math_functions.h"

int main() {
    int x = 10;
    int y = 5;
    
    printf("Сумма: %d\n", add(x, y));
    printf("Разность: %d\n", subtract(x, y));
    
    return 0;
}
